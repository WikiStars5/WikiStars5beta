
"use client";

// This component is no longer needed as its functionality has been integrated directly into FigureForm.
// It can be safely deleted. This empty version prevents build errors.
export function EnrichInfoButton() {
  return null;
}
