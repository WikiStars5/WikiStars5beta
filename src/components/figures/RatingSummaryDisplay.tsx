
"use client";

// This component is intentionally left blank.
// The rating summary display was removed from the UI as per user request
// to simplify the feature set and resolve persistent issues.
export function RatingSummaryDisplay() {
  return null;
}
