
'use server';

// This file is no longer needed as the AI flow is now called directly from the client component.
// It can be safely deleted. This empty version prevents build errors.
export async function enrichAndSaveFigureData() {
  // Deprecated
}
