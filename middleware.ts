// This middleware is disabled to simplify the routing structure.
// Internationalization can be re-enabled later if needed.
export function middleware() {}

export const config = {
  matcher: [],
};
